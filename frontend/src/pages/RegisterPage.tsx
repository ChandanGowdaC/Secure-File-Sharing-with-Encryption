/** Set 1 – F.1 Registration with client-side DH key generation. */

export default function RegisterPage() {
  return (
    <section>
      <h1>Register</h1>
      <p>Generate long-term DH key pair locally and register your public key with the server.</p>
      {/* TODO: registration form + generateLongTermKeyPair() */}
    </section>
  )
}
