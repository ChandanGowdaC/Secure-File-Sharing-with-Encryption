/** Set 1 – F.2 Login and F.3 MFA. */

export default function LoginPage() {
  return (
    <section>
      <h1>Login</h1>
      <p>Authenticate with credentials, then complete mandatory MFA.</p>
      {/* TODO: login form + MFA step */}
    </section>
  )
}
