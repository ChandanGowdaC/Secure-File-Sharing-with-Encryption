/** Set 3 – F.15 Admin transfer log (metadata only). */

export default function AdminPage() {
  return (
    <section>
      <h1>Admin – Transfer Log</h1>
      <p>View sender, receiver, transfer ID, timestamp, and status. No plaintext or keys.</p>
      {/* TODO: fetch /admin/logs and render table with filters */}
    </section>
  )
}
